one:
  email: user@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  username: userone
  notify_through_email: true

two:
  email: user2@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
  username: usertwo
  notify_through_email: false
